# UBID Mesh - Deployment Configuration

# Backend (Render)
services:
  - type: web
    name: ubidmesh-api
    runtime: python
    buildCommand: pip install -r backend/requirements.txt && python -m backend.seed
    startCommand: uvicorn backend.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
